[mypy]
python_version = 3.12
ignore_missing_imports = True
# Exclude build/ (packaged copies) to avoid duplicate-module errors when running mypy in-tree
exclude = ^build(/|\\)
files = splurge_unittest_to_pytest
